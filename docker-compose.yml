version: '3.8'
services:
  app:
    build: .
    ports:
      - "3012:3012"
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/app:delegated
      - /app/node_modules
      - ./data.db:/app/data.db
    command: npm run dev
    stdin_open: true
    tty: true
